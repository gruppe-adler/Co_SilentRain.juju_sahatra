 params ["_plane", "_unit"];

private _boundingbox = 3 boundingBoxReal _plane; 
_boundingbox params ["_mins", "_maxs", "_radius"];

_mins params ["_minX", "_minY", "_minZ"];
_maxs params ["_maxX", "_maxY", "_maxZ"];

private _maxWidth = abs ((_maxX) - (_minX));
private _maxLength = abs ((_maxY) - (_minY));
private _maxHeight = abs ((_maxZ) - (_minZ));

private _isInside = _unit inArea [_plane, _maxWidth/2, _maxLength/2, getDir _plane, true, _maxHeight/2];
private _return = [0,0,0];

if (_isInside) then {
	_return = (_plane worldToModelVisual ASLToAGL (getPosWorld _unit));
	systemChat ("debug: " + str _unit + " is inside");
	diag_log ("debug: " + str _unit + " is inside");
} else {
	systemChat ("debug: " + str _unit + " is NOT inside");
	diag_log ("debug: " + str _unit + " is NOT inside");
};

_return